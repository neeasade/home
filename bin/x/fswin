#!/bin/sh

sw=`res -w`
sh=`res -h`

read -r borderpx </tmp/info/wm/borders/size

activewin=`xdotool getactivewindow`

xdotool key alt + shift + space

xdotool windowmove ${activewin} 0	 0
xdotool windowsize ${activewin} ${sw} ${sh}
