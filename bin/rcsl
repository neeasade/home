#!/bin/bash
# script to recompile suckless software

base_dir="$HOME/usr/src"
sltools="dwm dmenu st tabbed"

for sltool in $sltools; do
    cd ${base_dir}/${sltool}
    echo "compiling ${sltool}"
    make clean install
done

[ -n $(pgrep bar) ] && pkill bar
$HOME/bin/bar &
