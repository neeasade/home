#!/bin/dash
# a battery thingy
base_dir='/sys/class/power_supply/'

for i in 0 1 2 3; do
    [ -d "${base_dir}/BAT${i}" ] && { base_dir="${base_dir}/BAT${i}"; break; }
done

case ${1} in
(-p) cat ${base_dir}/status;;
(-s) cat ${base_dir}/capacity;;
esac
