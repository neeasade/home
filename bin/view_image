#!/bin/dash
# view images from url using feh, dmenu and curl
tmp_image="/tmp/image.png"

download() {
    curl $1 > $tmp_image
}

show() {
    download $1
    feh $tmp_image
}

url="$(/dev/null | menu sel 'enter url:')"

show $url
wait
rm -rf $tmp_image
