#!/bin/dash
# get screen resolution

res="$(xrandr --nograb --current | awk '/\*/ {print $1}')"
res=${res% *}
sw=${res%x*}
sh=${res#*x}
sw=${sw%.*}
sh=${sh%.*}

case "$1" in
    -w) echo "${sw}";;
    -h) echo "${sh}";;
    *) echo "${sw} ${sh}";;
esac
