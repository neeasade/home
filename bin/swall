#!/bin/dash
# script to set wallpaper
swall() {
    cp "${2}" ${XDG_CACHE_HOME}/wall 2> /dev/null
    hsetroot -${1} "${2}"
    echo ${1} > ${HOME}/tmp/wall
}

case ${1} in
(set) swall $(cat ${HOME}/tmp/wall) ${XDG_CACHE_HOME}/wall;;
(*) swall ${1} "${2}";;
esac > /dev/null
