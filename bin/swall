#!/bin/dash
# script to set wallpaper
swall() {
    cp "$1" $XDG_CACHE_HOME/wall 2> /dev/null
    hsetroot -$2 "$1"
    echo $2 > $HOME/tmp/wall
}

case $1 in
    (-S) swall $XDG_CACHE_HOME/wall $(cat $HOME/tmp/wall);;
    (*) swall "$2" "$1";;
esac
