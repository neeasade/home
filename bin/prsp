#!/bin/sh

rf() {
    while read -r l; do
        echo "${l}"
    done < ${1}
}

style=`rf ${HOME}/etc/startpage/style.css`
h=`rf ${HOME}/etc/startpage/homepage.html`

echo "${h%%XCSS*}`rf ${HOME}/var/cache/tm/colors.css`\n${style##ICOLORS}${h##*XCSS}" \
     > ${HOME}/var/cache/homepage.html
