#!/bin/sh
# Add to/copy from dump file
export DUMP="${DUMP:-${HOME}/doc/dump}"

IFS='

'
set -- `cat ${DUMP}`

for i; do
	echo "${i}"
done | menu sel | {
	read -r sel
	grep -q "${sel}" ${DUMP} && echo "${sel}" | xin || echo "\n${sel}" >>${DUMP}
}
