#!/bin/dash
# window resize
# depends on wmutils and xdotool
wid=$(pfw)
ww=$(wattr w ${wid})
wh=$(wattr h ${wid})
w=${1}
h=${2}

[ ${w} != 'w' ] && nw=$(( ww ${w} )) || nw=w
[ ${h} != 'h' ] && nh=$(( wh ${h} )) || nh=h
xdotool getactivewindow windowsize ${nw} ${nh}
