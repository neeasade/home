#!/bin/sh
# wmutils script to center window
eval `xdotool getactivewindow getwindowgeometry --shell`

sw=$(res -w)
sh=$(res -h)

xdotool getactivewindow windowmove $(((sw - WIDTH) / 2)) $(((sh - HEIGHT) / 2))
