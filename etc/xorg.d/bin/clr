#!/bin/sh
# centered for 1080p screens

pgrep -x clrb 2>/dev/null && killall clrb

n=0
while read -r c; do
		(echo "";
		 sleep 10000000) | lemonbar -d -B ${c} -g 50x50+$((560 + 50 * n))+1000 \
									-n clrb -f "${XFONT}" &
	n=$((n + 1))
done <${XDG_CACHE_HOME}/tm/colors
