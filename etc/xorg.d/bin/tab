#!/bin/sh

xid_flag="${1}"
shift

get_color() {
    cat /tmp/info/dwm/colors/${1}
}

tabbed -o `get_color normbg` -O `get_color normfg` \
       -t `get_color selbg`  -T `get_color selfg`  \
       "${@}" "${xid_flag}"
